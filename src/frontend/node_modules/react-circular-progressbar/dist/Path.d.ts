/// <reference types="react" />
declare function Path({ className, counterClockwise, pathRadius, percentage, strokeWidth, style, }: {
    className?: string;
    counterClockwise: boolean;
    pathRadius: number;
    percentage: number;
    strokeWidth: number;
    style?: object;
}): JSX.Element;
export default Path;
